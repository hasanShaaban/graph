import 'package:flutter/widgets.dart';

class JopPostBodySec extends StatelessWidget {
  const JopPostBodySec({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}