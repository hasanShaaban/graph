import 'package:flutter/widgets.dart';

class GroupPostBodySec extends StatelessWidget {
  const GroupPostBodySec({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}